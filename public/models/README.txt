Place open-source face-api.js assets here for offline local inference:

1) Library file:
- face-api.min.js

2) Model weights/manifests for:
- tinyFaceDetector
- faceLandmark68Net
- faceRecognitionNet

Expected location:
/public/models/

The app loads ONLY from this local folder (no CDN, no paid API).
